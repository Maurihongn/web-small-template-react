import { createContext, useContext } from 'react';

// 1. Definimos el Estado
export interface {{pascalCase name}}State {
  isLoading: boolean;
  // TODO: Agrega tus propiedades aquí (ej: user: User | null)
}

// 2. Creamos el Contexto
export const {{pascalCase name}}Context = createContext<{{pascalCase name}}State | undefined>(undefined);

// 3. Hook de Consumo Seguro
export const use{{pascalCase name}} = () => {
  const context = useContext({{pascalCase name}}Context);
  if (!context) {
    throw new Error('use{{pascalCase name}} debe usarse dentro de un {{pascalCase name}}Provider');
  }
  return context;
};